Configuration:
  properties:
    property:
      - name: app
        value: account-balance

  Loggers:
    Root:
      level: DEBUG
      includeLocation: true
      AppenderRef:
        - ref: CONSOLE